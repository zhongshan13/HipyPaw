<style>
  /* 弹窗样式 */
  .quick-model-dialog {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: none; /* 默认隐藏 */
    justify-content: center;
    align-items: center;
    background: rgba(0, 0, 0, 0.5); /* 半透明遮罩 */
    z-index: 9999;
  }

  .quick-model-dialog.show {
    display: flex;
  }

  .quick-model-content {
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    width: 60%;
    /* max-width: 500px; */
    overflow-y: scroll;
    max-height: 700px;
    position: relative;
  }

  .quick-model-close {
    position: absolute;
    top: 0;
    right: 0;
    background: none;
    border: none;
    font-size: 30px;
    cursor: pointer;
  }
</style>

<div id="quick-model-dialog" class="quick-model-dialog">
  <div class="quick-model-content">
    <button class="quick-model-close">&times;</button>
    <div class="quick-model-body">
    </div>
  </div>
</div>
